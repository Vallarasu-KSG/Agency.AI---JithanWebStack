npm i react-router-dom
npm i lucide-react
npm i react-hot-toast

.hero,
.about,
.services,
.contact,
.footer {
  background: var(--bg-color);
  color: var(--text-color);
}

or

.card {
  background: var(--card-bg);
  color: var(--text-color);
  border: 1px solid var(--border-color);
}
இப்படி var(--bg-color) மற்றும் var(--text-color) மட்டும் use பண்ணினா முழு website-க்கும் Dark Mode / Light Mode automatic ஆக வேலை செய்யும். 🚀